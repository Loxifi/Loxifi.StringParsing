A collection of helpful string extension methods for reducing the complexity of common actions.

Includes methods like:

"string".From(value);
"string".To(value);
"string".Left(5);
"string".Right(2);

Additional information will be provided on full release